# 管理员用户名列表（逗号分隔，大小写不敏感）
# 在注册时，用户名与此列表匹配的账号将自动获得管理员权限
ADMIN_USERNAMES=admin,owner

# 数据库路径（可选，默认为 data/litematic.db）
# DB_PATH=/app/data/litematic.db

# 是否在 Cookie 上启用 Secure 标志（仅 HTTPS 发送）
# 默认生产环境开启。如果通过 HTTP 直接访问（如 http://IP:端口），请设置为 false
COOKIE_SECURE=false
